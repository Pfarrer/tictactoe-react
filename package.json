{
  "name": "tic-tac-toe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/server",
    "packages/app",
    "packages/shared"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest --coverage",
    "lint": "eslint .",
    "preview": "vite preview"
  }
}
